services:
  prometheus-agent:
    command:
    - --agent
    - --config.file=/etc/prometheus/prometheus.yml
    - --storage.agent.path=/prometheus
    - --storage.agent.retention.max-time=1d
    - --web.page-title=Prometheus Agent - Promstack
    - --web.console.libraries=/usr/share/prometheus/console_libraries
    - --web.console.templates=/usr/share/prometheus/consoles
    - --log.level=info
    configs:
    - source: prometheus-agent-config-tmpl
      target: /etc/prometheus/prometheus.yml
    - source: prometheus-agent-cadvisor
      target: /etc/prometheus/scrape-configs/cadvisor.yml
    - source: prometheus-agent-docker
      target: /etc/prometheus/scrape-configs/docker.yml
    - source: prometheus-agent-dockerswarm-services
      target: /etc/prometheus/scrape-configs/dockerswarm-services.yml
    - source: prometheus-agent-node-exporter
      target: /etc/prometheus/scrape-configs/node_exporter.yml
    - source: prometheus-agent-prometheus
      target: /etc/prometheus/scrape-configs/prometheus.yml
    deploy:
      mode: global
      labels:
        io.prometheus.dockerswarm-services.should_be_probed: "false"
        io.prometheus.dockerswarm-tasks.should_be_scraped: "false"
        io.prometheus.role: prometheus-agent
      update_config:
        parallelism: 1
        delay: 30s
        failure_action: rollback
        monitor: 15s
        max_failure_ratio: 0.2
      rollback_config:
        parallelism: 1
        delay: 30s
        failure_action: rollback
        monitor: 15s
        max_failure_ratio: 0.2
      resources:
        limits:
          memory: "1073741824"
      restart_policy:
        condition: any
        delay: 30s
        max_attempts: 15
        window: 15s
    environment:
      DOCKERSWARM_NODE_HOSTNAME: '{{.Node.Hostname}}'
      DOCKERSWARM_NODE_ID: '{{.Node.ID}}'
      PROMETHEUS_AGENT_CLUSTER_REPLICA: '{{ index .Service.Labels "com.docker.stack.namespace"}}'
    extra_hosts:
    - host.docker.internal:host-gateway
    hostname: replica-{{.Task.Slot}}.federated.prometheus.internal
    healthcheck:
      test:
      - CMD-SHELL
      - wget -qO - --tries=1 --spider http://127.0.0.1:9090/-/healthy || exit 1
      timeout: 10s
      interval: 30s
      retries: 3
      start_period: 30s
    image: docker.io/prom/prometheus:v3.0.0
    logging:
      driver: json-file
      options:
        max-file: "5"
        max-size: 12m
    networks:
      prometheus: null
      prometheus_gwnetwork: null
    ports:
    - mode: host
      target: 9090
      published: 19090
    user: "0:0"
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock
      read_only: true
    - type: volume
      source: prometheus-agent-data
      target: /prometheus
networks:
  prometheus:
    name: prometheus
    external: true
  prometheus_gwnetwork:
    name: prometheus_gwnetwork
    external: true
volumes:
  prometheus-agent-data: {}
configs:
  prometheus-agent-cadvisor:
    name: prometheus-agent-cadvisor-v1
    file: ./prometheus/scrape-configs/cadvisor.yml
    template_driver: golang
  prometheus-agent-config-tmpl:
    name: prometheus-agent-config-tmpl-v1
    file: ./prometheus/prometheus.yml.tmpl
    template_driver: golang
  prometheus-agent-docker:
    name: prometheus-agent-docker-v1
    file: ./prometheus/scrape-configs/docker.yml
    template_driver: golang
  prometheus-agent-dockerswarm-services:
    name: prometheus-agent-dockerswarm-services-v1
    file: ./prometheus/scrape-configs/dockerswarm-services.yml
    template_driver: golang
  prometheus-agent-node-exporter:
    name: prometheus-agent-node-exporter-v1
    file: ./prometheus/scrape-configs/node-exporter.yml
    template_driver: golang
  prometheus-agent-prometheus:
    name: prometheus-agent-prometheus-v1
    file: ./prometheus/scrape-configs/prometheus.yml
    template_driver: golang
x-default-logging:
  driver: json-file
  options:
    max-file: "5"
    max-size: 12m
x-deploy:
  mode: global
  resources:
    limits:
      memory: 1G
x-healthcheck:
  interval: 30s
  retries: 3
  start_period: 30s
  timeout: 10s

apiVersion: 1

datasources:
  - name: Prometheus (Promstack)
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus.svc.cluster.local:9090
    version: 1
    isDefault: true
    editable: true
    readOnly: false
